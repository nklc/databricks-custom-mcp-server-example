# This is a Databricks asset bundle definition for databricks-custom-mcp-server-example.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: databricks-custom-mcp-server-example

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: <your-databricks-workspace-host>